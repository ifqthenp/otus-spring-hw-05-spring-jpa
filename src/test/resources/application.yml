spring:
  main:
    banner-mode: "off"
  datasource:
    data: test-data.sql
    schema: test-schema.sql
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
       show_sql: false
